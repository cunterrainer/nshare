A command line application to send files and folders over the internet

Protocol: framed binary messages (v1) with file metadata, chunks, and per-file MD5 hash.
This is a breaking change from the previous ad-hoc delimiter protocol.

Use:
```
dart pub get
```

```
dart run bin/nshare.dart --help
```